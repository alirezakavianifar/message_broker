# Proxy Service Environment Configuration Template
# Copy this file to .env and update with your actual values

# Redis Configuration
# Redis runs locally on the proxy server
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# Main Server Configuration
# URL of the main server that the proxy will connect to
MAIN_SERVER_URL=https://173.32.115.223:8000

# Certificate Paths
# Paths relative to the proxy directory (where the service runs)
SERVER_CERT_PATH=certs/proxy.crt
SERVER_KEY_PATH=certs/proxy.key
CA_CERT_PATH=certs/ca.crt

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE_PATH=/opt/message_broker_proxy/proxy/logs

# Optional: Override default port (if different from 8001)
# PROXY_PORT=8001

# Optional: Override worker count (default: 4)
# WORKERS=4

